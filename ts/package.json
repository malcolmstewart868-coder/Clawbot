{
  "name": "ts",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "runner": "ts-node --project tsconfig.json app/runner.ts",
    "api": "ts-node --project tsconfig.json api/server.ts",
    "dev": "ts-node --project tsconfig.json app/runner.ts",
    "loop": "npm run bot",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.2.0",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
