# Clawbot

Clawbot is the Malcolm × Erbalis trading system:
- Guardrails-first execution
- Compounding discipline
- Observer mode by default

## Structure
- /ts = main runtime (TypeScript)
- /py = offline lab (Python, no execution)
- /shared/schema = shared candle/trade/journal formats
- /docs = project archive and notes
