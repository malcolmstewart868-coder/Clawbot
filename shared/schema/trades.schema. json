{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Trade",
  "type": "object",
  "required": [
    "id",
    "symbol",
    "side",
    "entry",
    "initialStop",
    "size",
    "status"
  ],
  "properties": {
    "id": { "type": "string" },
    "symbol": { "type": "string" },
    "side": { "type": "string", "enum": ["long", "short"] },
    "entry": { "type": "number" },
    "initialStop": { "type": "number" },
    "currentStop": { "type": ["number", "null"] },
    "size": { "type": "number" },
    "status": {
      "type": "string",
      "enum": ["open", "partial", "closed", "halted"]
    }
  }
}
